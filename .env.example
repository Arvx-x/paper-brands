# OpenAI-compatible gateway (Bifrost). All LLM traffic routes through here.
PB_BASE_URL=https://bifrost.clinikally.work/v1
PB_API_KEY=sk-replace-me
# Default model alias as configured in the Bifrost routing rules
PB_MODEL=gpt-5.2

# Optional: separate, cheaper model for high-volume persona simulation
PB_SIM_MODEL=gpt-5.2-mini

# Concurrency for arena simulations
PB_CONCURRENCY=6
